import React, { useContext } from "react"
import { AuthContext } from "../../context"
import "../../dist/dashboard.scss"

export default function Dashboard() {
  return <div className="dashboard-app">Dashboard</div>
}
